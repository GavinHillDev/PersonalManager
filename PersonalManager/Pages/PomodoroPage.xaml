<Page x:Class="PersonalManager.Pages.PomodoroPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:vm="clr-namespace:PersonalManager.ViewModels"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PersonalManager.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PomodoroPage">
    <Page.DataContext>
        <vm:PomodoroViewModel/>
    </Page.DataContext>
    <Grid Name="parentGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="99*"/>
            <ColumnDefinition Width="202*"/>
            <ColumnDefinition Width="99*"/>
        </Grid.ColumnDefinitions>

        <Button Grid.Column="2" Width="100" Height="100px" Background="AliceBlue" Command="{Binding ChangeTextCommand}" Content="Click Me" />
        <TextBlock Grid.Column="1" Width="100" Height="100px" Text="{Binding DisplayText}"></TextBlock>
    </Grid>
</Page>
